---
- name: Install Pip
  ansible.builtin.apt:
    name: python3-pip
    state: present
    update_cache: yes
- name: Install virtualenv
  ansible.builtin.apt:
    name: python3-venv
    state: present